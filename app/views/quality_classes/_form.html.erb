<%= form_for(@quality_class) do |f| %>
  <% if @quality_class.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@quality_class.errors.count, "error") %> prohibited this quality_class from being saved:</h2>

      <ul>
      <% @quality_class.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
